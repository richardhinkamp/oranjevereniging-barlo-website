{% extends "layout.twig" %}

{% block title_text %}Sponsoring voor Groot Deunk{% endblock %}

{% block page_content %}
    <div class="row">
        <div class="box">
            <div class="col-lg-12">
                <hr>
                <h2 class="intro-text text-center">Sponsoring voor Groot Deunk</h2>
                <hr>
            </div>
            <div class="col-lg-12">
                <p>Heel fijn dat je ons wilt steunen! Controleer onderstaande overzicht goed en als alles klopt kun je betalen via iDEAL. Klopt er iets niet? <a href="javascript:window.history.back();">Ga dan terug en corrigeer de gegevens.</a></p>
                <dl>
                    <dt>Naam</dt>
                    <dd>{{ name }}</dd>
                    <dt>E-mail adres</dt>
                    <dd>{{ email }}</dd>
                    <dt>Aantal bouwstenen</dt>
                    <dd>{{ bouwstenen }} a € 50,- (renteloze lening)</dd>
                    <dt>Gift</dt>
                    <dd>€&nbsp;{{ gift }},-</dd>
                    <dt>Totaal</dt>
                    <dd>€&nbsp;{{ amount }},-</dd>
                </dl>
                <form action="/sponsor" method="POST" class="sponsor">
                    <input type="hidden" name="name" value="{{ name }}">
                    <input type="hidden" name="email" value="{{ email }}">
                    <input type="hidden" name="bouwstenen" value="{{ bouwstenen }}">
                    <input type="hidden" name="gift" value="{{ gift }}">
                    <input type="hidden" name="added" value="{{ added }}">
                    <input type="hidden" name="confirm" value="1">
                    <input type="submit" value="Bevestig en betaal">
                </form>
            </div>
            <div class="clearfix"></div>
        </div>
    </div>
{% endblock %}